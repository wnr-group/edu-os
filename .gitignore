.DS_Store
node_modules/
.next/
dist/
.turbo/
.env
.env.local
*.tsbuildinfo
.worktrees/
test-screenshots/
.vercel
apps/demo-video/
.superpowers/

# Mobile build artifacts (Android app bundles / packages — upload-only, huge)
*.aab
*.apk

# Play Store listing assets (icons, graphics, screenshots — not source)
play-assets/

# npm lockfile — this is a pnpm workspace (pnpm-lock.yaml is the source of truth)
package-lock.json

# Supabase Studio SQL Editor scratch queries — often written against real data
supabase/snippets/
